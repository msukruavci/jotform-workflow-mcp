[
  {
    "name": "show_workflows",
    "description": "Show the user's workflows in the interactive workflow list UI.\n\n        Use this presentation tool when the user asks to see, browse, list,\n        or choose from their workflows. It reads Jotform directly; never build\n        its payload from assistant prose or remembered tool results.",
    "input_schema": {
      "properties": {},
      "title": "show_workflowsArguments",
      "type": "object"
    }
  },
  {
    "name": "show_workflow",
    "description": "Show one workflow in the interactive read-only workflow preview UI.\n\n        Use when the user asks to open, show, preview, or inspect a workflow.\n        Also call this exactly once after all requested workflow creation or\n        update operations have finished and their final state was read back.\n        Do not call it for intermediate write steps.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "Workflow id from list_workflows, create_workflow, or a write result.",
          "title": "Workflow Id",
          "type": "string"
        }
      },
      "required": [
        "workflow_id"
      ],
      "title": "show_workflowArguments",
      "type": "object"
    }
  },
  {
    "name": "list_step_types",
    "description": "List the workflow step types you can add to a workflow.\n\n        Each entry gives an add_step step_type, the name the Jotform builder\n        shows for it (ui_name), and a one-line description. Some UI entries\n        are variants with a canonical_type and subtype; pass the listed\n        step_type directly to add_step.\n\n        schema_available=false means this server has no field schema for that\n        type: it can appear in an existing workflow, but get_step_schema will\n        not describe it. Call get_step_schema on a type before configuring it.",
    "input_schema": {
      "properties": {
        "category": {
          "default": "",
          "description": "Optional filter — \"basic\" (email, task, approval), \"logic\" (conditions, branching, loops), \"ai\" (AI-powered steps), or \"integration\" (webhooks, payments, signing). Leave empty to see all.",
          "title": "Category",
          "type": "string"
        }
      },
      "title": "list_step_typesArguments",
      "type": "object"
    }
  },
  {
    "name": "get_step_schema",
    "description": "Get the configurable fields for one or more workflow step types.\n\n        Returns field names, types, allowed values and descriptions. Where a\n        field takes a list of objects, item_fields shows what one item holds.\n        Positioning fields are omitted — those are handled automatically. For\n        multiple unfamiliar types, pass step_types=[...] to fetch every schema\n        in one tool roundtrip.",
    "input_schema": {
      "properties": {
        "step_type": {
          "default": "",
          "description": "Single step type name (e.g. \"workflow_send_email\").",
          "title": "Step Type",
          "type": "string"
        },
        "step_types": {
          "default": [],
          "description": "Optional list of step type names to retrieve schemas for in a single batch call.",
          "items": {
            "type": "string"
          },
          "title": "Step Types",
          "type": "array"
        }
      },
      "title": "get_step_schemaArguments",
      "type": "object"
    }
  },
  {
    "name": "list_workflows",
    "description": "List the user's workflows.\n\n        Returns each workflow's id, title, status, when it was last updated,\n        and run_count — how many times it has actually run, which is the\n        quickest way to tell a live workflow from an abandoned draft.\n\n        Use the id with get_workflow to see the steps.",
    "input_schema": {
      "properties": {},
      "title": "list_workflowsArguments",
      "type": "object"
    }
  },
  {
    "name": "get_workflow",
    "description": "Get a workflow's structure: its steps, how they connect, and whether\n        the structure is sound.\n\n        Each connection carries an `outcome` when it leaves a branching step:\n        TRUE or FALSE on an if/else, the branch name on a conditional\n        branch, or the button/outcome text on an approval/task. That is how\n        you tell two paths apart. A split's paths are equivalent and carry\n        no outcome.\n\n        `health` reports steps that can never run (unreachable from the start\n        point), steps that lead nowhere, branches defined but wired to nothing,\n        and step types this server has no schema for. A workflow can look fine\n        as a list and still be broken.\n\n        Steps are summaries only — use get_step_details for one step's full\n        configuration.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "From list_workflows.",
          "title": "Workflow Id",
          "type": "string"
        }
      },
      "required": [
        "workflow_id"
      ],
      "title": "get_workflowArguments",
      "type": "object"
    }
  },
  {
    "name": "get_step_details",
    "description": "Get the full configuration of a single step — subject line, recipients,\n        condition terms, and so on.\n\n        get_workflow only summarizes steps; use this when you need to know\n        exactly how one step is set up, for example before changing it.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "From list_workflows.",
          "title": "Workflow Id",
          "type": "string"
        },
        "step_id": {
          "description": "From get_workflow's steps list.",
          "title": "Step Id",
          "type": "string"
        }
      },
      "required": [
        "workflow_id",
        "step_id"
      ],
      "title": "get_step_detailsArguments",
      "type": "object"
    }
  },
  {
    "name": "list_workflow_revisions",
    "description": "List saved workflow revisions for this MCP server session/history.\n\n        Revisions are full snapshots captured automatically before mutating\n        tools write to Jotform. Use restore_workflow_revision to preview and\n        restore one of them.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "From list_workflows.",
          "title": "Workflow Id",
          "type": "string"
        },
        "limit": {
          "default": 10,
          "description": "Maximum revisions to return, newest first. Default 10.",
          "title": "Limit",
          "type": "integer"
        }
      },
      "required": [
        "workflow_id"
      ],
      "title": "list_workflow_revisionsArguments",
      "type": "object"
    }
  },
  {
    "name": "list_forms",
    "description": "List the user's forms, with each form's id, title, status and\n        submission count.\n\n        A workflow is triggered by one of these, so this is usually the first\n        call when working out which form a workflow is about.",
    "input_schema": {
      "properties": {},
      "title": "list_formsArguments",
      "type": "object"
    }
  },
  {
    "name": "search_workflow_templates",
    "description": "Search and inspect the Jotform workflow template catalog using local FAISS RAG vector similarity.\n\n        Use this tool whenever the user asks for workflow ideas, inspiration, or template\n        recommendations, or when asked to design/create a new workflow for any domain\n        (e.g., leave request, approvals, onboarding, expense, feedback) to discover proven\n        architectures and step patterns. Returns the top matching template blueprints directly;\n        do not call a separate template-detail tool afterward.",
    "input_schema": {
      "properties": {
        "query": {
          "description": "Natural language query describing the desired workflow idea, domain, or process (e.g. 'employee vacation approval flow', 'purchase order approval with signature').",
          "title": "Query",
          "type": "string"
        },
        "top_k": {
          "default": 2,
          "description": "Number of relevant template blueprints to return (default 2, max 3)",
          "title": "Top K",
          "type": "integer"
        }
      },
      "required": [
        "query"
      ],
      "title": "search_workflow_templatesArguments",
      "type": "object"
    }
  },
  {
    "name": "create_form_with_ai",
    "description": "Create a new Jotform form from an AI prompt.\n\n        Use this only after the user chooses \"create a new form\" for a\n        workflow trigger and describes what the form should collect.",
    "input_schema": {
      "properties": {
        "prompt": {
          "description": "Natural-language description of the form to create. Ask the user what fields, labels, language, and purpose they want before calling this. The created form can be passed to create_workflow as trigger_form_id.",
          "title": "Prompt",
          "type": "string"
        },
        "form_type": {
          "default": "classic",
          "description": "Form type preference for Jotform AI. Default \"classic\".",
          "title": "Form Type",
          "type": "string"
        },
        "language": {
          "default": "en",
          "description": "Form language preference. Default \"en\"; use \"tr\" when the user wants Turkish.",
          "title": "Language",
          "type": "string"
        },
        "intent": {
          "default": "",
          "description": "Optional short, privacy-conscious summary of the user's intent for audit/debug logs. Do not copy the full user message; keep one phrase.",
          "title": "Intent",
          "type": "string"
        },
        "reason": {
          "default": "",
          "description": "Optional short explanation of why this tool call is the right next step. Used for audit/debug logs and revision history.",
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "prompt"
      ],
      "title": "create_form_with_aiArguments",
      "type": "object"
    }
  },
  {
    "name": "build_workflow_bulk",
    "description": "Primary tool for building workflows in one bulk operation.\n\n        Use this tool for both:\n        - Creating a new workflow from scratch by passing title/form_prompt or title/trigger_form_id.\n        - Adding a complete graph of steps and connections to an existing workflow_id.\n\n        If the user asks for a new workflow and does not provide an existing form ID,\n        pass form_prompt here. Do not create the trigger form with any separate\n        Jotform Form plugin/tool, and do not stop after creating only a form.\n\n        This is the model-facing workflow creation path; standalone workflow\n        creation helpers are hidden and only used as internal fallback pieces.\n        Do not use form_prompt as a standalone form creation step; include steps and connections in the same call.\n        All step references ('ref') in steps can be wired using 'from_ref' and 'to_ref' in connections.\n        Use 'start' or '1' as from_ref to connect from the trigger form. When updating an existing workflow,\n        existing step IDs from get_workflow can also be used in from_ref/to_ref.",
    "input_schema": {
      "$defs": {
        "ConnectionSpec": {
          "properties": {
            "from_ref": {
              "description": "The source step's ref name (e.g. 'start', '1', or 'approval_1'). 'start' or '1' refers to the trigger form start point. When updating an existing workflow, an existing Jotform step_id from get_workflow is also accepted.",
              "title": "From Ref",
              "type": "string"
            },
            "to_ref": {
              "description": "The target step's ref name (e.g. 'approval_1', 'notify_mgr'). When updating an existing workflow, an existing Jotform step_id from get_workflow is also accepted.",
              "title": "To Ref",
              "type": "string"
            },
            "outcome": {
              "default": "",
              "description": "Required if from_ref is a branching step: if/else, conditional branch, approval, or task with outcomes (e.g. 'Approve', 'Deny', 'TRUE', 'FALSE'). Leave empty for non-branching steps.",
              "title": "Outcome",
              "type": "string"
            }
          },
          "required": [
            "from_ref",
            "to_ref"
          ],
          "title": "ConnectionSpec",
          "type": "object"
        },
        "StepSpec": {
          "properties": {
            "ref": {
              "description": "A unique temporary reference name for this step in the bulk request (e.g. 'approval_1', 'notify_mgr', 'reject_email').",
              "title": "Ref",
              "type": "string"
            },
            "type": {
              "description": "From list_step_types, e.g. \"workflow_approval\", \"workflow_send_email\".",
              "title": "Type",
              "type": "string"
            },
            "config": {
              "additionalProperties": true,
              "description": "Fields for this step type — check get_step_schema. For condition terms in build_workflow_bulk, prefer the trigger form's visible field label. Known field_id/qid/name values are also accepted; the bulk tool resolves them after creating/reading the trigger form and refuses ambiguous labels instead of guessing.",
              "title": "Config",
              "type": "object"
            }
          },
          "required": [
            "ref",
            "type"
          ],
          "title": "StepSpec",
          "type": "object"
        }
      },
      "properties": {
        "workflow_id": {
          "default": "",
          "description": "Optional. ID of an existing workflow to add steps to. If omitted, a new workflow is created automatically using title/form_prompt.",
          "title": "Workflow Id",
          "type": "string"
        },
        "steps": {
          "description": "List of steps to create. Each step has a unique 'ref' name (e.g. 'approval_1', 'notify_mgr', 'reject_email'), 'type' (e.g. 'workflow_approval', 'workflow_send_email', 'workflow_conditional_branch'), and 'config' dict.",
          "items": {
            "$ref": "#/$defs/StepSpec"
          },
          "title": "Steps",
          "type": "array"
        },
        "connections": {
          "default": [],
          "description": "List of connections between steps. 'from_ref' can be 'start' (or '1') for the trigger form, any new step 'ref', or an existing step_id when workflow_id is provided. 'to_ref' is the target step's 'ref' or an existing step_id. 'outcome' is required for branching steps (e.g. 'Approve', 'Deny', 'TRUE', 'FALSE', or branch name). When inserting before an existing END, reuse that END step_id as the final to_ref instead of creating a duplicate END.",
          "items": {
            "$ref": "#/$defs/ConnectionSpec"
          },
          "title": "Connections",
          "type": "array"
        },
        "title": {
          "default": "",
          "description": "Optional. Name of the workflow when creating a new one.",
          "title": "Title",
          "type": "string"
        },
        "trigger_form_id": {
          "default": "",
          "description": "Optional. Existing form ID to bind as trigger form when creating a new workflow. Use only when the user explicitly provides an existing form ID or asks to use an existing form; otherwise use form_prompt so this Workflow MCP creates the trigger form.",
          "title": "Trigger Form Id",
          "type": "string"
        },
        "form_prompt": {
          "default": "",
          "description": "Optional. Natural language description to automatically generate an AI trigger form for this workflow. Use this for new trigger forms instead of any separate Jotform Form plugin/tool. Include the expected form purpose and fields. Use only in the same build_workflow_bulk call that includes the complete steps and connections; do not call this tool with form_prompt only or empty steps.",
          "title": "Form Prompt",
          "type": "string"
        },
        "form_language": {
          "default": "en",
          "description": "Form language for AI trigger form generation. Use \"tr\" for Turkish, otherwise \"en\".",
          "title": "Form Language",
          "type": "string"
        },
        "intent": {
          "default": "",
          "description": "Optional short, privacy-conscious summary of the user's intent for audit/debug logs. Do not copy the full user message; keep one phrase.",
          "title": "Intent",
          "type": "string"
        },
        "reason": {
          "default": "",
          "description": "Optional short explanation of why this tool call is the right next step. Used for audit/debug logs and revision history.",
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "steps"
      ],
      "title": "build_workflow_bulkArguments",
      "type": "object"
    }
  },
  {
    "name": "delete_step",
    "description": "Delete a step from a workflow. Irreversible.\n\n        First call (confirm=false) changes nothing and returns the step's\n        type, label, and every connection that would break. Second call\n        (confirm=true) actually deletes it.\n\n        Confirmed (probes/test_delete_impact.py, 2026-08-10): Jotform does\n        NOT remove a step's connections when the step is deleted — this\n        tool deletes them explicitly, in the same request, so no broken\n        links are left behind.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "From list_workflows.",
          "title": "Workflow Id",
          "type": "string"
        },
        "step_id": {
          "description": "From get_workflow's steps list.",
          "title": "Step Id",
          "type": "string"
        },
        "confirm": {
          "default": false,
          "description": "Leave false to preview what deleting this step would affect. Only pass true after showing that preview to the user and getting their explicit go-ahead — never set this based on your own judgement that it's probably fine.",
          "title": "Confirm",
          "type": "boolean"
        },
        "intent": {
          "default": "",
          "description": "Optional short, privacy-conscious summary of the user's intent for audit/debug logs. Do not copy the full user message; keep one phrase.",
          "title": "Intent",
          "type": "string"
        },
        "reason": {
          "default": "",
          "description": "Optional short explanation of why this tool call is the right next step. Used for audit/debug logs and revision history.",
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "workflow_id",
        "step_id"
      ],
      "title": "delete_stepArguments",
      "type": "object"
    }
  },
  {
    "name": "publish_workflow",
    "description": "Publish a workflow, making it live — from this point on, matching\n        form submissions actually run it.\n\n        This runs the same health check as get_workflow — unreachable steps,\n        dead ends, unlabelled branches — records warnings in the result, saves\n        a revision, then publishes immediately.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "From list_workflows.",
          "title": "Workflow Id",
          "type": "string"
        },
        "intent": {
          "default": "",
          "description": "Optional short, privacy-conscious summary of the user's intent for audit/debug logs. Do not copy the full user message; keep one phrase.",
          "title": "Intent",
          "type": "string"
        },
        "reason": {
          "default": "",
          "description": "Optional short explanation of why this tool call is the right next step. Used for audit/debug logs and revision history.",
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "workflow_id"
      ],
      "title": "publish_workflowArguments",
      "type": "object"
    }
  },
  {
    "name": "restore_workflow_revision",
    "description": "Restore a workflow to a saved revision.\n\n        Revisions are captured automatically before add/update/connect/\n        disconnect/delete/publish operations. Without revision_id, this uses\n        the newest saved revision, so it is the \"go back one change\" tool.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "From list_workflows.",
          "title": "Workflow Id",
          "type": "string"
        },
        "revision_id": {
          "default": "",
          "description": "Optional. If empty, restores the newest saved revision for this workflow, usually the state immediately before the last mutating tool call.",
          "title": "Revision Id",
          "type": "string"
        },
        "confirm": {
          "default": false,
          "description": "Leave false to preview the restore target. Only pass true after showing that preview to the user and getting explicit approval. The current workflow is backed up as a new revision before restore.",
          "title": "Confirm",
          "type": "boolean"
        },
        "intent": {
          "default": "",
          "description": "Optional short, privacy-conscious summary of the user's intent for audit/debug logs. Do not copy the full user message; keep one phrase.",
          "title": "Intent",
          "type": "string"
        },
        "reason": {
          "default": "",
          "description": "Optional short explanation of why this tool call is the right next step. Used for audit/debug logs and revision history.",
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "workflow_id"
      ],
      "title": "restore_workflow_revisionArguments",
      "type": "object"
    }
  },
  {
    "name": "delete_workflow",
    "description": "Delete an entire workflow. Irreversible, and bigger than delete_step\n        — this removes every step in it.\n\n        First call (confirm=false) changes nothing and returns the title\n        to show the user. Second call needs both confirm=true and\n        confirm_title matching exactly.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "From list_workflows.",
          "title": "Workflow Id",
          "type": "string"
        },
        "confirm": {
          "default": false,
          "description": "Leave false to preview. Only true after the user explicitly says to proceed.",
          "title": "Confirm",
          "type": "boolean"
        },
        "confirm_title": {
          "default": "",
          "description": "Required alongside confirm=true — must exactly match the workflow's current title (returned in the preview). This exists because workflow titles are often similar or duplicated; matching by id alone made it easy to delete the wrong one by picking the wrong list index. Read the title back to the user and use what they confirm, don't retype it from memory.",
          "title": "Confirm Title",
          "type": "string"
        },
        "intent": {
          "default": "",
          "description": "Optional short, privacy-conscious summary of the user's intent for audit/debug logs. Do not copy the full user message; keep one phrase.",
          "title": "Intent",
          "type": "string"
        },
        "reason": {
          "default": "",
          "description": "Optional short explanation of why this tool call is the right next step. Used for audit/debug logs and revision history.",
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "workflow_id"
      ],
      "title": "delete_workflowArguments",
      "type": "object"
    }
  }
]