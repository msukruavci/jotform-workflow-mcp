[
  {
    "name": "show_workflows",
    "description": "Show the user's workflows in the interactive workflow list UI.\n\n        Use this presentation tool when the user asks to see, browse, list,\n        or choose from their workflows. It reads Jotform directly; never build\n        its payload from assistant prose or remembered tool results.",
    "input_schema": {
      "properties": {},
      "title": "show_workflowsArguments",
      "type": "object"
    }
  },
  {
    "name": "show_workflow",
    "description": "Show one workflow in the interactive read-only workflow preview UI.\n\n        Use when the user asks to open, show, preview, or inspect a workflow.\n        Also call this exactly once after all requested workflow creation or\n        update operations have finished and their final state was read back.\n        Do not call it for intermediate write steps.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "Workflow id from list_workflows, create_workflow, or a write result.",
          "title": "Workflow Id",
          "type": "string"
        }
      },
      "required": [
        "workflow_id"
      ],
      "title": "show_workflowArguments",
      "type": "object"
    }
  },
  {
    "name": "list_step_types",
    "description": "List the workflow step types you can add to a workflow.\n\n        Each entry gives an add_step step_type, the name the Jotform builder\n        shows for it (ui_name), and a one-line description. Some UI entries\n        are variants with a canonical_type and subtype; pass the listed\n        step_type directly to add_step.\n\n        schema_available=false means this server has no field schema for that\n        type: it can appear in an existing workflow, but get_step_schema will\n        not describe it. Call get_step_schema on a type before configuring it.",
    "input_schema": {
      "properties": {
        "category": {
          "default": "",
          "description": "Optional filter — \"basic\" (email, task, approval), \"logic\" (conditions, branching, loops), \"ai\" (AI-powered steps), or \"integration\" (webhooks, payments, signing). Leave empty to see all.",
          "title": "Category",
          "type": "string"
        }
      },
      "title": "list_step_typesArguments",
      "type": "object"
    }
  },
  {
    "name": "get_step_schema",
    "description": "Get the configurable fields for one or more workflow step types.\n\n        Returns field names, types, allowed values and descriptions. Where a\n        field takes a list of objects, item_fields shows what one item holds.\n        Positioning fields are omitted — those are handled automatically. For\n        multiple unfamiliar types, pass step_types=[...] to fetch every schema\n        in one tool roundtrip.",
    "input_schema": {
      "properties": {
        "step_type": {
          "default": "",
          "description": "Single step type name (e.g. \"workflow_send_email\").",
          "title": "Step Type",
          "type": "string"
        },
        "step_types": {
          "default": [],
          "description": "Optional list of step type names to retrieve schemas for in a single batch call.",
          "items": {
            "type": "string"
          },
          "title": "Step Types",
          "type": "array"
        }
      },
      "title": "get_step_schemaArguments",
      "type": "object"
    }
  },
  {
    "name": "list_workflows",
    "description": "List the user's workflows.\n\n        Returns each workflow's id, title, status, when it was last updated,\n        and run_count — how many times it has actually run, which is the\n        quickest way to tell a live workflow from an abandoned draft.\n\n        Use the id with get_workflow to see the steps.",
    "input_schema": {
      "properties": {},
      "title": "list_workflowsArguments",
      "type": "object"
    }
  },
  {
    "name": "get_workflow",
    "description": "Get a workflow's structure: its steps, how they connect, and whether\n        the structure is sound.\n\n        Each connection carries an `outcome` when it leaves a branching step:\n        TRUE or FALSE on an if/else, the branch name on a conditional\n        branch, or the button/outcome text on an approval/task. That is how\n        you tell two paths apart. A split's paths are equivalent and carry\n        no outcome.\n\n        `health` reports steps that can never run (unreachable from the start\n        point), steps that lead nowhere, branches defined but wired to nothing,\n        and step types this server has no schema for. A workflow can look fine\n        as a list and still be broken.\n\n        Steps are summaries only — use get_step_details for one step's full\n        configuration.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "From list_workflows.",
          "title": "Workflow Id",
          "type": "string"
        }
      },
      "required": [
        "workflow_id"
      ],
      "title": "get_workflowArguments",
      "type": "object"
    }
  },
  {
    "name": "get_step_details",
    "description": "Get the full configuration of a single step — subject line, recipients,\n        condition terms, and so on.\n\n        get_workflow only summarizes steps; use this when you need to know\n        exactly how one step is set up, for example before changing it.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "From list_workflows.",
          "title": "Workflow Id",
          "type": "string"
        },
        "step_id": {
          "description": "From get_workflow's steps list.",
          "title": "Step Id",
          "type": "string"
        }
      },
      "required": [
        "workflow_id",
        "step_id"
      ],
      "title": "get_step_detailsArguments",
      "type": "object"
    }
  },
  {
    "name": "list_workflow_revisions",
    "description": "List saved workflow revisions for this MCP server session/history.\n\n        Revisions are full snapshots captured automatically before mutating\n        tools write to Jotform. Use restore_workflow_revision to preview and\n        restore one of them.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "From list_workflows.",
          "title": "Workflow Id",
          "type": "string"
        },
        "limit": {
          "default": 10,
          "description": "Maximum revisions to return, newest first. Default 10.",
          "title": "Limit",
          "type": "integer"
        }
      },
      "required": [
        "workflow_id"
      ],
      "title": "list_workflow_revisionsArguments",
      "type": "object"
    }
  },
  {
    "name": "inspect_workflow_gaps",
    "description": "Check for incomplete workflow setup before continuing or publishing.\n\n        This catches empty links, unwired branch outcomes, missing assignees,\n        missing email/task content, and condition fields that are not real\n        fields on the trigger form. Call this before presenting a workflow as\n        ready, and when the user asks what still needs to be completed.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "From list_workflows.",
          "title": "Workflow Id",
          "type": "string"
        }
      },
      "required": [
        "workflow_id"
      ],
      "title": "inspect_workflow_gapsArguments",
      "type": "object"
    }
  },
  {
    "name": "list_forms",
    "description": "List the user's forms, with each form's id, title, status and\n        submission count.\n\n        A workflow is triggered by one of these, so this is usually the first\n        call when working out which form a workflow is about.",
    "input_schema": {
      "properties": {},
      "title": "list_formsArguments",
      "type": "object"
    }
  },
  {
    "name": "get_form_fields",
    "description": "List a form's fields (questions), with each field's id, label, type\n        and whether it is required.\n\n        Needed for two things: picking which field a condition should test,\n        and picking which field holds the email address when sending mail to\n        the person who submitted the form.",
    "input_schema": {
      "properties": {
        "form_id": {
          "description": "From list_forms.",
          "title": "Form Id",
          "type": "string"
        }
      },
      "required": [
        "form_id"
      ],
      "title": "get_form_fieldsArguments",
      "type": "object"
    }
  },
  {
    "name": "search_workflow_templates",
    "description": "Search the Jotform workflow template catalog using local FAISS RAG vector similarity.\n\n        Use this tool whenever the user asks for workflow ideas, inspiration, or template\n        recommendations, or when asked to design/create a new workflow for any domain\n        (e.g., leave request, approvals, onboarding, expense, feedback) to discover proven\n        architectures and step patterns.",
    "input_schema": {
      "properties": {
        "query": {
          "description": "Natural language query describing the desired workflow idea, domain, or process (e.g. 'employee vacation approval flow', 'purchase order approval with signature').",
          "title": "Query",
          "type": "string"
        },
        "top_k": {
          "default": 3,
          "description": "Number of relevant template references to return (default 3, max 10)",
          "title": "Top K",
          "type": "integer"
        }
      },
      "required": [
        "query"
      ],
      "title": "search_workflow_templatesArguments",
      "type": "object"
    }
  },
  {
    "name": "get_workflow_template",
    "description": "Retrieve the full architectural blueprint of a workflow template.\n\n        Returns complete element configurations (names, types, approval outcomes/buttons,\n        assigned roles, email templates, timeouts) and edge links connecting each step.\n        Use this to inspect exact configurations or replicate a template's flow structure.",
    "input_schema": {
      "properties": {
        "template_id": {
          "description": "The unique ID of the template to inspect in detail (e.g. '242943285550056').",
          "title": "Template Id",
          "type": "string"
        }
      },
      "required": [
        "template_id"
      ],
      "title": "get_workflow_templateArguments",
      "type": "object"
    }
  },
  {
    "name": "create_form_with_ai",
    "description": "Create a new Jotform form from an AI prompt.\n\n        Use this only after the user chooses \"create a new form\" for a\n        workflow trigger and describes what the form should collect.",
    "input_schema": {
      "properties": {
        "prompt": {
          "description": "Natural-language description of the form to create. Ask the user what fields, labels, language, and purpose they want before calling this. The created form can be passed to create_workflow as trigger_form_id.",
          "title": "Prompt",
          "type": "string"
        },
        "form_type": {
          "default": "classic",
          "description": "Form type preference for Jotform AI. Default \"classic\".",
          "title": "Form Type",
          "type": "string"
        },
        "language": {
          "default": "en",
          "description": "Form language preference. Default \"en\"; use \"tr\" when the user wants Turkish.",
          "title": "Language",
          "type": "string"
        },
        "intent": {
          "default": "",
          "description": "Optional short, privacy-conscious summary of the user's intent for audit/debug logs. Do not copy the full user message; keep one phrase.",
          "title": "Intent",
          "type": "string"
        },
        "reason": {
          "default": "",
          "description": "Optional short explanation of why this tool call is the right next step. Used for audit/debug logs and revision history.",
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "prompt"
      ],
      "title": "create_form_with_aiArguments",
      "type": "object"
    }
  },
  {
    "name": "create_workflow",
    "description": "Low-level helper to create a new workflow with a trigger form.\n\n        For ordinary new-workflow requests, prefer build_workflow_bulk with\n        title/form_prompt or title/trigger_form_id so creation and step wiring\n        happen in one tool call. Use this tool only for manual/partial setup.\n\n        1. Use an existing form — then call list_forms and pass the chosen\n           form id as trigger_form_id.\n        2. Create a new form first — prefer build_workflow_bulk with form_prompt.\n        3. No trigger form yet — only proceed with allow_without_trigger=true\n           if the user explicitly asks for a draft workflow without one.\n\n        Returns the new workflow_id. ALWAYS use build_workflow_bulk to add\n        all steps and connections in one atomic shot. DO NOT call add_step in a loop.",
    "input_schema": {
      "properties": {
        "title": {
          "description": "Workflow name.",
          "title": "Title",
          "type": "string"
        },
        "trigger_form_id": {
          "default": "",
          "description": "Required unless allow_without_trigger=true — the form (from list_forms) whose submissions should trigger this workflow. Before calling create_workflow, decide the form strategy with the user: existing form or new AI-created form. For an existing form, call list_forms first and use the selected id here. For a complete new workflow, prefer build_workflow_bulk with form_prompt/title instead of this low-level tool. Binding takes two API calls under the hood and is verified by reading the start point back.",
          "title": "Trigger Form Id",
          "type": "string"
        },
        "allow_without_trigger": {
          "default": false,
          "description": "Default false. Set true only if the user explicitly asks for a draft workflow with no trigger form. Normal workflows must have a trigger form.",
          "title": "Allow Without Trigger",
          "type": "boolean"
        },
        "intent": {
          "default": "",
          "description": "Optional short, privacy-conscious summary of the user's intent for audit/debug logs. Do not copy the full user message; keep one phrase.",
          "title": "Intent",
          "type": "string"
        },
        "reason": {
          "default": "",
          "description": "Optional short explanation of why this tool call is the right next step. Used for audit/debug logs and revision history.",
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "title"
      ],
      "title": "create_workflowArguments",
      "type": "object"
    }
  },
  {
    "name": "create_workflow_with_ai_form",
    "description": "Low-level helper to create a new AI-generated form, then a workflow triggered by it.\n\n        For complete new workflows, prefer a single build_workflow_bulk call\n        with title, form_prompt, steps, and connections. Use this helper only\n        when the user explicitly wants form/workflow creation without steps yet.",
    "input_schema": {
      "properties": {
        "title": {
          "description": "Workflow name.",
          "title": "Title",
          "type": "string"
        },
        "form_prompt": {
          "description": "Natural-language description of the new trigger form to create before creating the workflow. Include the form's purpose and desired fields. Default language is English; mention Turkish only if the user asks for it.",
          "title": "Form Prompt",
          "type": "string"
        },
        "form_type": {
          "default": "classic",
          "description": "Form type preference for Jotform AI. Default \"classic\".",
          "title": "Form Type",
          "type": "string"
        },
        "language": {
          "default": "en",
          "description": "Form language preference. Default \"en\"; use \"tr\" when the user wants Turkish.",
          "title": "Language",
          "type": "string"
        },
        "intent": {
          "default": "",
          "description": "Optional short, privacy-conscious summary of the user's intent for audit/debug logs. Do not copy the full user message; keep one phrase.",
          "title": "Intent",
          "type": "string"
        },
        "reason": {
          "default": "",
          "description": "Optional short explanation of why this tool call is the right next step. Used for audit/debug logs and revision history.",
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "title",
        "form_prompt"
      ],
      "title": "create_workflow_with_ai_formArguments",
      "type": "object"
    }
  },
  {
    "name": "build_workflow_bulk",
    "description": "Primary tool for building workflows in one bulk operation.\n\n        Use this tool for both:\n        - Creating a new workflow from scratch by passing title/form_prompt or title/trigger_form_id.\n        - Adding a complete graph of steps and connections to an existing workflow_id.\n\n        Prefer this over create_workflow_with_ai_form followed by another bulk call for new workflows.\n        All step references ('ref') in steps can be wired using 'from_ref' and 'to_ref' in connections.\n        Use 'start' or '1' as from_ref to connect from the trigger form.",
    "input_schema": {
      "$defs": {
        "ConnectionSpec": {
          "properties": {
            "from_ref": {
              "description": "The source step's ref name (e.g. 'start', '1', or 'approval_1'). 'start' or '1' refers to the trigger form start point.",
              "title": "From Ref",
              "type": "string"
            },
            "to_ref": {
              "description": "The target step's ref name (e.g. 'approval_1', 'notify_mgr').",
              "title": "To Ref",
              "type": "string"
            },
            "outcome": {
              "default": "",
              "description": "Required if from_ref is a branching step: if/else, conditional branch, approval, or task with outcomes (e.g. 'Approve', 'Deny', 'TRUE', 'FALSE'). Leave empty for non-branching steps.",
              "title": "Outcome",
              "type": "string"
            }
          },
          "required": [
            "from_ref",
            "to_ref"
          ],
          "title": "ConnectionSpec",
          "type": "object"
        },
        "StepSpec": {
          "properties": {
            "ref": {
              "description": "A unique temporary reference name for this step in the bulk request (e.g. 'approval_1', 'notify_mgr', 'reject_email').",
              "title": "Ref",
              "type": "string"
            },
            "type": {
              "description": "From list_step_types, e.g. \"workflow_approval\", \"workflow_send_email\".",
              "title": "Type",
              "type": "string"
            },
            "config": {
              "additionalProperties": true,
              "description": "Fields for this step type — check get_step_schema.",
              "title": "Config",
              "type": "object"
            }
          },
          "required": [
            "ref",
            "type"
          ],
          "title": "StepSpec",
          "type": "object"
        }
      },
      "properties": {
        "workflow_id": {
          "default": "",
          "description": "Optional. ID of an existing workflow to add steps to. If omitted, a new workflow is created automatically using title/form_prompt.",
          "title": "Workflow Id",
          "type": "string"
        },
        "steps": {
          "description": "List of steps to create. Each step has a unique 'ref' name (e.g. 'approval_1', 'notify_mgr', 'reject_email'), 'type' (e.g. 'workflow_approval', 'workflow_send_email', 'workflow_conditional_branch'), and 'config' dict.",
          "items": {
            "$ref": "#/$defs/StepSpec"
          },
          "title": "Steps",
          "type": "array"
        },
        "connections": {
          "default": [],
          "description": "List of connections between steps. 'from_ref' can be 'start' (or '1') for the trigger form, or any step 'ref'. 'to_ref' is the target step's 'ref'. 'outcome' is required for branching steps (e.g. 'Approve', 'Deny', 'TRUE', 'FALSE', or branch name).",
          "items": {
            "$ref": "#/$defs/ConnectionSpec"
          },
          "title": "Connections",
          "type": "array"
        },
        "title": {
          "default": "",
          "description": "Optional. Name of the workflow when creating a new one.",
          "title": "Title",
          "type": "string"
        },
        "trigger_form_id": {
          "default": "",
          "description": "Optional. Existing form ID to bind as trigger form when creating a new workflow.",
          "title": "Trigger Form Id",
          "type": "string"
        },
        "form_prompt": {
          "default": "",
          "description": "Optional. Natural language description to automatically generate an AI trigger form for this workflow.",
          "title": "Form Prompt",
          "type": "string"
        },
        "form_language": {
          "default": "en",
          "description": "Form language for AI trigger form generation. Use \"tr\" for Turkish, otherwise \"en\".",
          "title": "Form Language",
          "type": "string"
        },
        "intent": {
          "default": "",
          "description": "Optional short, privacy-conscious summary of the user's intent for audit/debug logs. Do not copy the full user message; keep one phrase.",
          "title": "Intent",
          "type": "string"
        },
        "reason": {
          "default": "",
          "description": "Optional short explanation of why this tool call is the right next step. Used for audit/debug logs and revision history.",
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "steps"
      ],
      "title": "build_workflow_bulkArguments",
      "type": "object"
    }
  },
  {
    "name": "add_step",
    "description": "Add a single step to an EXISTING workflow for minor manual edits.\n\n        CRITICAL: DO NOT call add_step in a loop when creating or generating a workflow.\n        ALWAYS use build_workflow_bulk instead to build all steps and wiring in one atomic call.\n\n        Returns the new step_id. Position on the canvas is chosen automatically.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "From list_workflows.",
          "title": "Workflow Id",
          "type": "string"
        },
        "step_type": {
          "description": "From list_step_types, e.g. \"workflow_send_email\".",
          "title": "Step Type",
          "type": "string"
        },
        "config": {
          "additionalProperties": true,
          "description": "Fields for this step type — call get_step_schema first to see what it accepts. Unknown fields are dropped, not rejected; check `warnings` in the result. Before adding steps whose behavior depends on user intent — tasks, emails, approvals, conditions, conditional branches, or any step with outcomes/description/message fields — ask one short question for the missing essentials instead of creating an empty placeholder. Examples: for a task ask who it is assigned to and what should be done; for a branch ask the branch names and conditions; for an approval ask the approver and outcomes. The server refuses empty task/approval/assign/email/sign/condition steps.",
          "title": "Config",
          "type": "object"
        },
        "after_step_id": {
          "default": "",
          "description": "Optional — if given, connects this step directly after that one. Only works when after_step_id doesn't already have an outgoing connection (a step with more than one exit needs deliberate wiring — use connect_steps for that, with an outcome if the source branches).",
          "title": "After Step Id",
          "type": "string"
        },
        "allow_duplicate": {
          "default": false,
          "description": "Default false. Set true only after the user explicitly wants another similar step instead of reusing/updating/connecting the existing one.",
          "title": "Allow Duplicate",
          "type": "boolean"
        },
        "intent": {
          "default": "",
          "description": "Optional short, privacy-conscious summary of the user's intent for audit/debug logs. Do not copy the full user message; keep one phrase.",
          "title": "Intent",
          "type": "string"
        },
        "reason": {
          "default": "",
          "description": "Optional short explanation of why this tool call is the right next step. Used for audit/debug logs and revision history.",
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "workflow_id",
        "step_type",
        "config"
      ],
      "title": "add_stepArguments",
      "type": "object"
    }
  },
  {
    "name": "connect_steps",
    "description": "Connect two existing steps in an EXISTING workflow for minor manual edits.\n\n        CRITICAL: DO NOT call connect_steps in a loop when creating or generating a workflow.\n        ALWAYS use build_workflow_bulk instead to build all steps and connections in one atomic call.\n\n        Fails without changing anything if the outcome doesn't exist, is\n        already connected elsewhere, or is missing when required.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "From list_workflows.",
          "title": "Workflow Id",
          "type": "string"
        },
        "from_step_id": {
          "description": "From get_workflow's steps list.",
          "title": "From Step Id",
          "type": "string"
        },
        "to_step_id": {
          "description": "From get_workflow's steps list.",
          "title": "To Step Id",
          "type": "string"
        },
        "outcome": {
          "default": "",
          "description": "Required if from_step_id is a branching step: if/else, conditional branch, approval, or task with outcomes. Examples: \"TRUE\", \"FALSE\", \"Approve\", \"Reject\", or a custom task/branch outcome name. Check get_workflow or get_step_details on from_step_id to see what outcomes exist and which are already used. Leave empty only for steps without outcomes.",
          "title": "Outcome",
          "type": "string"
        },
        "intent": {
          "default": "",
          "description": "Optional short, privacy-conscious summary of the user's intent for audit/debug logs. Do not copy the full user message; keep one phrase.",
          "title": "Intent",
          "type": "string"
        },
        "reason": {
          "default": "",
          "description": "Optional short explanation of why this tool call is the right next step. Used for audit/debug logs and revision history.",
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "workflow_id",
        "from_step_id",
        "to_step_id"
      ],
      "title": "connect_stepsArguments",
      "type": "object"
    }
  },
  {
    "name": "disconnect_steps",
    "description": "Remove a single connection between two steps, without deleting\n        either step.\n\n        If the connection leaves a branching step (if/else, conditional\n        branch, approval, or task with outcomes), that outcome's link is cleared first — so it\n        shows up as unconnected again and can be wired to something else\n        with connect_steps. Without this, the outcome would still point at\n        a link_id that no longer exists: resolve_outcome would wrongly\n        report it as already connected, and get_workflow's health check\n        would separately start flagging it as a dangling link.\n\n        Use this instead of add_step + connect_steps when the goal is\n        rewiring an existing structure — for example, replacing\n        On Submission -> Review -> Approval with a direct\n        On Submission -> Approval by removing the old link first.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "From list_workflows.",
          "title": "Workflow Id",
          "type": "string"
        },
        "link_id": {
          "description": "From get_workflow's connections list.",
          "title": "Link Id",
          "type": "string"
        },
        "intent": {
          "default": "",
          "description": "Optional short, privacy-conscious summary of the user's intent for audit/debug logs. Do not copy the full user message; keep one phrase.",
          "title": "Intent",
          "type": "string"
        },
        "reason": {
          "default": "",
          "description": "Optional short explanation of why this tool call is the right next step. Used for audit/debug logs and revision history.",
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "workflow_id",
        "link_id"
      ],
      "title": "disconnect_stepsArguments",
      "type": "object"
    }
  },
  {
    "name": "update_step",
    "description": "Change an existing step's configuration.\n\n        Does not move the step or change its connections — use connect_steps\n        for wiring.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "From list_workflows.",
          "title": "Workflow Id",
          "type": "string"
        },
        "step_id": {
          "description": "From get_workflow's steps list.",
          "title": "Step Id",
          "type": "string"
        },
        "config": {
          "additionalProperties": true,
          "description": "Only the fields to change — call get_step_details first to see current values, get_step_schema for valid fields.",
          "title": "Config",
          "type": "object"
        },
        "intent": {
          "default": "",
          "description": "Optional short, privacy-conscious summary of the user's intent for audit/debug logs. Do not copy the full user message; keep one phrase.",
          "title": "Intent",
          "type": "string"
        },
        "reason": {
          "default": "",
          "description": "Optional short explanation of why this tool call is the right next step. Used for audit/debug logs and revision history.",
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "workflow_id",
        "step_id",
        "config"
      ],
      "title": "update_stepArguments",
      "type": "object"
    }
  },
  {
    "name": "delete_step",
    "description": "Delete a step from a workflow. Irreversible.\n\n        First call (confirm=false) changes nothing and returns the step's\n        type, label, and every connection that would break. Second call\n        (confirm=true) actually deletes it.\n\n        Confirmed (probes/test_delete_impact.py, 2026-08-10): Jotform does\n        NOT remove a step's connections when the step is deleted — this\n        tool deletes them explicitly, in the same request, so no broken\n        links are left behind.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "From list_workflows.",
          "title": "Workflow Id",
          "type": "string"
        },
        "step_id": {
          "description": "From get_workflow's steps list.",
          "title": "Step Id",
          "type": "string"
        },
        "confirm": {
          "default": false,
          "description": "Leave false to preview what deleting this step would affect. Only pass true after showing that preview to the user and getting their explicit go-ahead — never set this based on your own judgement that it's probably fine.",
          "title": "Confirm",
          "type": "boolean"
        },
        "intent": {
          "default": "",
          "description": "Optional short, privacy-conscious summary of the user's intent for audit/debug logs. Do not copy the full user message; keep one phrase.",
          "title": "Intent",
          "type": "string"
        },
        "reason": {
          "default": "",
          "description": "Optional short explanation of why this tool call is the right next step. Used for audit/debug logs and revision history.",
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "workflow_id",
        "step_id"
      ],
      "title": "delete_stepArguments",
      "type": "object"
    }
  },
  {
    "name": "publish_workflow",
    "description": "Publish a workflow, making it live — from this point on, matching\n        form submissions actually run it.\n\n        First call (confirm=false) changes nothing. It runs the same health\n        check as get_workflow — unreachable steps, dead ends, unlabelled\n        branches — and returns them as warnings. A workflow with warnings\n        can still be published; the warnings exist so the user finds out\n        about a broken branch from you, before it goes live, rather than\n        from a submission that silently went nowhere.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "From list_workflows.",
          "title": "Workflow Id",
          "type": "string"
        },
        "confirm": {
          "default": false,
          "description": "Leave false to check the workflow's structure and see what publishing would do. Only pass true after showing that to the user and getting their explicit go-ahead.",
          "title": "Confirm",
          "type": "boolean"
        },
        "intent": {
          "default": "",
          "description": "Optional short, privacy-conscious summary of the user's intent for audit/debug logs. Do not copy the full user message; keep one phrase.",
          "title": "Intent",
          "type": "string"
        },
        "reason": {
          "default": "",
          "description": "Optional short explanation of why this tool call is the right next step. Used for audit/debug logs and revision history.",
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "workflow_id"
      ],
      "title": "publish_workflowArguments",
      "type": "object"
    }
  },
  {
    "name": "restore_workflow_revision",
    "description": "Restore a workflow to a saved revision.\n\n        Revisions are captured automatically before add/update/connect/\n        disconnect/delete/publish operations. Without revision_id, this uses\n        the newest saved revision, so it is the \"go back one change\" tool.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "From list_workflows.",
          "title": "Workflow Id",
          "type": "string"
        },
        "revision_id": {
          "default": "",
          "description": "Optional. If empty, restores the newest saved revision for this workflow, usually the state immediately before the last mutating tool call.",
          "title": "Revision Id",
          "type": "string"
        },
        "confirm": {
          "default": false,
          "description": "Leave false to preview the restore target. Only pass true after showing that preview to the user and getting explicit approval. The current workflow is backed up as a new revision before restore.",
          "title": "Confirm",
          "type": "boolean"
        },
        "intent": {
          "default": "",
          "description": "Optional short, privacy-conscious summary of the user's intent for audit/debug logs. Do not copy the full user message; keep one phrase.",
          "title": "Intent",
          "type": "string"
        },
        "reason": {
          "default": "",
          "description": "Optional short explanation of why this tool call is the right next step. Used for audit/debug logs and revision history.",
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "workflow_id"
      ],
      "title": "restore_workflow_revisionArguments",
      "type": "object"
    }
  },
  {
    "name": "delete_workflow",
    "description": "Delete an entire workflow. Irreversible, and bigger than delete_step\n        — this removes every step in it.\n\n        First call (confirm=false) changes nothing and returns the title\n        to show the user. Second call needs both confirm=true and\n        confirm_title matching exactly.",
    "input_schema": {
      "properties": {
        "workflow_id": {
          "description": "From list_workflows.",
          "title": "Workflow Id",
          "type": "string"
        },
        "confirm": {
          "default": false,
          "description": "Leave false to preview. Only true after the user explicitly says to proceed.",
          "title": "Confirm",
          "type": "boolean"
        },
        "confirm_title": {
          "default": "",
          "description": "Required alongside confirm=true — must exactly match the workflow's current title (returned in the preview). This exists because workflow titles are often similar or duplicated; matching by id alone made it easy to delete the wrong one by picking the wrong list index. Read the title back to the user and use what they confirm, don't retype it from memory.",
          "title": "Confirm Title",
          "type": "string"
        },
        "intent": {
          "default": "",
          "description": "Optional short, privacy-conscious summary of the user's intent for audit/debug logs. Do not copy the full user message; keep one phrase.",
          "title": "Intent",
          "type": "string"
        },
        "reason": {
          "default": "",
          "description": "Optional short explanation of why this tool call is the right next step. Used for audit/debug logs and revision history.",
          "title": "Reason",
          "type": "string"
        }
      },
      "required": [
        "workflow_id"
      ],
      "title": "delete_workflowArguments",
      "type": "object"
    }
  }
]